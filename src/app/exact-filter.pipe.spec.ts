import { ExactFilterPipe } from './exact-filter.pipe';

describe('ExactFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExactFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
